<div class="row container d-flex justify-content-evenly mx-auto">
    {{#each productos}}
        <div class="card col-md-4 my-3" style="width: 18rem;">
            <img src="{{this.img}}" class="card-img-top" alt="...">
            <div class="card-body">
                <h3 class="card-title text-center pb-2 fw-bold">{{this.nombre}} </h3>  
                <a href="#" class="btn btn-primary w-100 agregar" data-id={{this.id}} >AÑADIR</a>
            </div>
        </div>
    {{/each}}
</div>

